[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
db = { path = "../db" }
net = { path = "../net" }
protocol = { path = "../protocol" }
runtime = { path = "../runtime" }

[dev-dependencies]
libc = "*"
